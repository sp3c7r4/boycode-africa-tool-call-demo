import { generateText, ToolLoopAgent } from "ai"

`
Function Calling Fundamentals
Teaching an LLM to use external tools instead of guessing answers it can't know
`

/** @info - Initialize deepseek provider */


const BoycodeAgent = generateText({
  model: ,
})
